# tools/*.py 가 사용하는 Python 의존성.
# 신규 환경 세팅:
#   python3 -m venv .venv
#   source .venv/bin/activate
#   pip install -r requirements.txt
google-auth>=2.49,<3
requests>=2.33,<3
# era_boundaries 폴리곤 생성 — Natural Earth GeoJSON 을 시대별 bounding box 로
# 클립해 정밀한 해안선 폴리곤을 만든다 (tools/seed/build_era_boundaries_from_geojson.py).
shapely>=2.0,<3
# Vertex Imagen 데코 PNG 후처리 — 평면 흰색 배경을 chroma-key 로 투명화
# (tools/images/generate_decos_vertex.py).
Pillow>=10.0,<12
