# Story Bible — legacy Claude entrypoint

This repository now uses `AGENTS.md` as the primary agent instruction file.

Claude-specific files under `.claude/` are kept only as historical reference.
For current workflows, domain guidance, validation commands, and Codex skill
locations, read `AGENTS.md` first.
